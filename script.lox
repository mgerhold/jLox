var a = 10;
{
    fun printA() { // closure
        print a;
    }

    printA();

    var a = 42; // shadowing

    printA();
}

// Lexer (Scanner) -> static syntax analysis (Parser (=> syntax tree)) -> static semantic analysis -> Interpreter